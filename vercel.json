{
  "buildCommand": "npx ng build --configuration=production --optimization=false",
  "outputDirectory": "dist/gerenciamento-tarefas-angular",
  "devCommand": "ng serve",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
